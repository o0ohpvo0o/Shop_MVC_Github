@model List<Model.EF.ProductCategory>

<div class="categories">
    <ul>
        <h3>Categories</h3>
        @foreach (var item in Model)
        {
            <li><a href="/products/@item.MetaTitle-@item.ID">@item.Name</a></li>
        }
    </ul>
</div>
